angular.module('app.sell', [])

.controller('SellController', function($scope, $location) {
  $scope.test = "hello testing sell";

  

  // if (!Auth.isAuth()) {
  //   $location.path('/signin');
  // } else {
  //   $scope.getQuestion();
  // }
});